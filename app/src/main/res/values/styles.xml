<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="RoundedImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">5dp</item> <!-- Ajusta el radio aquí -->
    </style>

    <style name="MaterialAlertDialog" parent="Theme.MaterialComponents.Light.Dialog.Alert">
        <item name="android:titleTextColor">@color/violeta_fuerte</item>
        <item name="android:textColorPrimary">@color/violeta_bajo</item>
        <item name="android:buttonTint">@color/violeta_fuerte</item>
        <item name="android:textColor">@color/violeta_fuerte</item>
        <item name="android:background">@color/white</item>
        <item name="android:dialogCornerRadius" tools:ignore="NewApi">10dp</item>
    </style>

</resources>